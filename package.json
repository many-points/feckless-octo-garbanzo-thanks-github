{
  "name": "chrome-extention",
  "version": "1.0.0",
  "description": "",
  "main": "content.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "many-points",
  "license": "ISC",
  "devDependencies": {
    "coffee-script": "^1.10.0"
  },
  "scripts": {
    "prestart": "coffee --compile --bare --output dist src",
    "start": "coffee src/content.coffee"
  }
}
